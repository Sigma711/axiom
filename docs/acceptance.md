# 验收范围

本任务以修改前已核对的本地与远程共同提交 `6e3cf4c2c77cdc848c0f55d31e5a93d245497246` 为基线。用户授权直接推送，主分支必须为 main。

1. 修复并完善数据探索、回测、模拟盘、策略对比；各模块都能正确实践知识大全的所有概念。
2. 覆盖用户提供的 83 页《股票交易软件专业指标全解_完整版.pdf》的全部内容，重复名称可以映射到同一概念，但不能遗漏含义不同的子概念。
3. 每个知识概念必须有准确公式、真实可执行代码、精确 GitHub 行级链接，代码移动后构建自动重定位；图文并茂、深入浅出、简洁、小白友好。
4. 展开详情后显示收起详情；两种状态文字与箭头清楚区分，浅色和深色主题颜色协调可读。
5. 所有下拉框只有一个展开箭头；所有控件、图标、图表在浅深主题及窄屏下正确、协调、可读。
6. 严谨测试覆盖单元、公共模块接口、HTTP、WebSocket、浏览器实际操作与视觉检查，全部纳入 CI。
7. 既然选择 Make，构建、运行、测试、检查和覆盖率统一由 Make 管理。
8. 验收前完成本地全部检查、推送 main、验证远程 CI，并重启核对 8080 的实际前后端及用户看到的浏览器。

## 项目约定

遵循全局 AGENTS.md，应用适当 Matt Pocock 技能，多代理独立执行及审阅。测试从公共接口验证可观察行为，数值期望使用独立例子；不删除或跳过失败测试，不使用全局压制警告。不得用伪造外部财务/链上数据、空泛通用算式或占位图冒充每个概念的实现。无法从 OHLCV 推出的输入明确标注为可编辑教学示例，未定义值及资料边界明确解释。
